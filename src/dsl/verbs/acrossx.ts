import { Verb } from '../../types/verb';
import { ids } from './ids';

export namespace acrossx {
  export const evaluated: Verb = {
    display: {
      und: 'evaluated',
      'es-ES': 'evaluó',
      'en-US': 'evaluated'
    },
    id: ids.acrossx.evaluated
  };

  export const posted: Verb = {
    display: {
      und: 'posted',
      'es-ES': 'posteó',
      'en-US': 'posted'
    },
    id: ids.acrossx.posted
  };
}
