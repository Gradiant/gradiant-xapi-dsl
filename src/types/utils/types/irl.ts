/**
 * Internationalized Resource Location, or IRIs, are unique identifiers which could also be resolvable.
 * Because resolving is not a requirement, IRLs/URLs are used instead of IRLs/URLs. In order to allow the greatest flexibility in the
 * characters used in an identifier, IRIs are used instead of URIs as IRLs can contain some characters outside of the ASCII character set
 */
export type IRL = string;
