/**
 * Universally Unique Identifiers, or UUIDs, are 128-bit values that are globally unique. Unlike IRIs, there is no expectation of
 * resolvability as UUIDs take on a completely different format. UUIDs MUST be in the standard string form.
 * It is recommended variant 2 in RFC 4122 is used.
 */
export type UUID = string;
